var wHeight = "innerHeight" in window
               ? window.innerHeight
               : document.documentElement.offsetHeight;
var wWidth = "innerWidth" in window
               ? window.innerWidth
               : document.documentElement.offsetWidth;
export const whandww = {
     wHeight:wHeight,
     wWidth:wWidth
}
